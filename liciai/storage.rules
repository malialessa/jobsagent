rules_version = '2';

service firebase.storage {
match /b/{bucket}/o {
// Permite leitura e escrita para todos os usuários autenticados.
// Você pode restringir isso ainda mais para caminhos específicos se necessário.
match /{allPaths=**} {
allow read, write: if request.auth != null;
}
}
}